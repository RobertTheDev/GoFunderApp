import IRouterRoute from '../../../interfaces/RouterRoute';
import HomePage from '../pages/HomePage';

const homeRouter: IRouterRoute[] = [{ path: '/', element: <HomePage /> }];

export default homeRouter;
